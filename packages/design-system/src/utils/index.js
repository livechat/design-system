import generateNotificationUniqueId from './generateNotificationUniqueId';
import callAll from './callAll';
import getMergedClassNames from './getMergedClassNames';

const helpers = {
  generateNotificationUniqueId,
  callAll,
  getMergedClassNames
};

export default helpers;
