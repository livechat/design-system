import { Meta, Controls } from '@storybook/blocks';

import { HeadingExamples } from './components/HeadingExamples';
import { TextExamples } from './components/TextExamples';
import { DisplayExamples } from './components/DisplayExamples';
import { Card } from '../../../components/Card';

<Meta title="Foundations/Typography" />

# Typography

This guide will help you create a typography hierarchy in the interface you build. This can be achieved by using size and weight.

## Heading

`Heading` component renders heading element with given size.

You can modify the output element via `as` prop. For example if you want to render XLarge heading as `h3` just pass `as='h3'`

```jsx
<Heading size="xl" as="h3">
  My h3 heading
</Heading>
```

[//]: # '<Controls /> TODO: add controls after we will create a story for Heading component'

<Card
  className="sb-unstyled"
  style={{
    color: 'var(--content-default)',
    backgroundColor: 'var(--background)',
  }}
>
  <HeadingExamples />
</Card>

## Text

`Text` component renders a paragraph.

```jsx
<Text size="sm" bold>
  This text is a small bold p
</Text>
```

[//]: # '<Controls /> TODO: add controls after we will create a story for Text component'

<Card
  className="sb-unstyled"
  style={{
    color: 'var(--content-default)',
    backgroundColor: 'var(--background)',
  }}
>
  <TextExamples />
</Card>

## Display

`Display` component renders a div.

```jsx
<Display size="sm">This text is a small div</Display>
```

[//]: # '<Controls /> TODO: add controls after we will create a story for Display component'

<Card
  className="sb-unstyled"
  style={{
    color: 'var(--content-default)',
    backgroundColor: 'var(--background)',
  }}
>
  <DisplayExamples />
</Card>
