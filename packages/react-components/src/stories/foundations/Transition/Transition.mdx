import { Meta } from '@storybook/blocks';
import { TransitionTable } from './TransitionTable';
import {
  TransitionDelayTokens,
  TransitionDurationTokens,
  TransitionTimingTokens,
} from './constants';
import { TransitionDurationExample } from './components/TransitionDurationExample';
import { TransitionTimingExample } from './components/TransitionTimingExample';
import { TransitionDelayExample } from './components/TransitionDelayExample';
import { Card } from '../../../components/Card';

<Meta title="Foundations/Transition" />

# Transition

Transitions are used to animate changes in an element's style. They are often used to make elements appear more smoothly or to create a visual effect.

## Usage

To use spacing tokens in your project, you need to import the specific transition constant from `@livechat/design-system-react-components`:

Currently, the following transition tokens are available:

- TransitionDurationToken
- TransitionTimingToken
- TransitionDelayToken

```jsx
import {
  TransitionDurationToken,
  TransitionTimingToken,
  TransitionDelayToken,
} from '@livechat/design-system-react-components';
```

```css
.my-element {
  transition: var(${TransitionDurationToken.Fast1}) var(
      ${TransitionTimingToken.EaseIn}
    );
}
```

## Duration

<Card className="sb-unstyled">
  <TransitionDurationExample />
</Card>

<TransitionTable data={TransitionDurationTokens} />

## Timing

Animation with **Ease-in** - start slow and speed up towards the end.

Animation with **Ease-out** - fast start and slow end.

<Card className="sb-unstyled">
  <TransitionTimingExample />
</Card>

<TransitionTable data={TransitionTimingTokens} />

## Delay

Use it to determine the time it takes to start an animation or trigger a component

<Card className="sb-unstyled">
  <TransitionDelayExample />
</Card>

<TransitionTable data={TransitionDelayTokens} />
