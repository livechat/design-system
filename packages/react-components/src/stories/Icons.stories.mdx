import { Meta, Canvas, ArgsTable } from '@storybook/addon-docs';
import { linkTo } from '@storybook/addon-links';

import { Icon } from '../components/Icon';
import { Alert } from '../components/Alert';
import { IconsShowcase } from './components/IconsShowcase';

<Meta title="Deprecated/Material Icons" />

<Alert
  kind="warning"
  primaryButton={{
    label: 'Browse Icons',
    handleClick: linkTo('Foundations/Tabler Icons')
  }}
>
  This icons library is <b>deprecated</b>, it will be not updated anymore, 
  and in the future will be removed. We strongly recommend you to start 
  using <b>new Tabler icons</b>.
</Alert>

#

# Material Icons (deprecated)

List of the icons available in the design system. <br />
Use `@livechat/design-system-icons/react/material`

<Canvas
  style={{
    color: 'var(--content-default)',
    backgroundColor: 'var(--background)',
  }}
>
  <IconsShowcase />
</Canvas>
