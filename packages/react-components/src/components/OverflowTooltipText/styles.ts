import { css } from '@emotion/css';

export const overflowTooltipText = css`
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
`;

export const tooltipContent = css`
  word-break: break-word;
`;
